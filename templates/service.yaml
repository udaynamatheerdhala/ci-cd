apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo_app.fullname" . }}
  labels:
    {{- include "demo_app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo_app.selectorLabels" . | nindent 4 }}
